org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Xray core runtime version (dev.tfox.fluttervless:xray-android on Maven Central).
# Updated automatically by .github/workflows/update-core.yml when the plugin
# author publishes a newer build. If the pinned version is not yet on Maven
# Central, build a local override with scripts/build_xray_android.sh; Gradle
# resolves android/xray-maven before mavenCentral.
flutterVlessXrayRuntimeVersion=26.7.28
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
