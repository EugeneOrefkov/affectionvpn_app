org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Xray core runtime version — uses the flutter_vless plugin's default build.
# Set to 26.7.11 which matches the plugin's bundled core. The latest prerelease
# is only available via the experimental core panel (5-tap Easter egg).
# If this version is not yet on Maven Central, build a local override with
# scripts/build_xray_android.sh; Gradle resolves android/xray-maven first.
flutterVlessXrayRuntimeVersion=26.7.11
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
