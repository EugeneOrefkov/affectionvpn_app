org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Xray core runtime version — the dev.tfox.fluttervless:xray-android AAR
# published on Maven Central. Must be an existing published version, since
# Gradle resolves it straight from Maven Central.
# Experimental cores built from Xray-core sources are installed at runtime
# via the experimental core panel (5-tap Easter egg).
flutterVlessXrayRuntimeVersion=26.7.11
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
